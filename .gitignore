# ── Dependencies ────────────────────────────────────────
node_modules/

# ── Build output ────────────────────────────────────────
dist/
src-tauri/target/

# ── Local cargo config (OneDrive workaround, machine-specific) ──
src-tauri/.cargo/config.toml

# ── OS files ────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Editor ──────────────────────────────────────────────
.vscode/*
!.vscode/extensions.json
*.swp
*.swo

# ── Environment ─────────────────────────────────────────
.env
.env.*

# ── Large CUDA/GPU binaries (tracked via Git LFS) ──────
# These are required at runtime for GPU-accelerated whisper.
# Managed by Git LFS due to GitHub's 100 MB file size limit.

# ── Lock files managed by package manager ───────────────
# Keep package-lock.json tracked (needed for npm ci)
